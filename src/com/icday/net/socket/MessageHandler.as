package com.icday.net.socket
{
	import morn.core.handlers.Handler;
	
	public class MessageHandler extends Handler
	{
		public function MessageHandler()
		{
			super(method, args);
		}
		
		protected function onMessage(cmd:int, data:Object):void{
			
		}
	}
}